{
  "name": "wasm-wasi-overview",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Node.js examples that walk through the four WebAssembly import/export kinds (function, global, memory, table) and a small WASI program showing how imports resolve to ELF symbols.",
  "type": "module",
  "scripts": {
    "example:function": "node function/function.mjs",
    "example:global": "node global/global.mjs",
    "example:memory": "node memory/memory.mjs",
    "example:table": "node table/table.mjs",
    "examples": "npm run example:function && npm run example:global && npm run example:memory && npm run example:table"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
